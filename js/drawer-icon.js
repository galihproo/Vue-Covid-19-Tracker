var logo = bodymovin.loadAnimation({
    container: document.getElementById('logo'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'js/logo.json' // the path to the animation json
});

var iconWash = bodymovin.loadAnimation({
    container: document.getElementById('icon-wash-hand'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'js/wash-hand.json' // the path to the animation json
});

var iconMask = bodymovin.loadAnimation({
    container: document.getElementById('icon-facemask'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'js/facemask.json' // the path to the animation json
});

var iconGel = bodymovin.loadAnimation({
    container: document.getElementById('icon-gel'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'js/gel.json' // the path to the animation json
});

var iconHand = bodymovin.loadAnimation({
    container: document.getElementById('icon-hand'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'js/hand.json' // the path to the animation json
});

var iconQuarantine = bodymovin.loadAnimation({
    container: document.getElementById('icon-quarantine'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'js/quarantine.json' // the path to the animation json
});